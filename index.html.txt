<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>0xTanda // Official Store</title>
    
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Load React & ReactDOM -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    
    <!-- Load Babel -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    
    <!-- Google Fonts (Fallback untuk Preview) -->
    <!-- Space Grotesk (Mirip Vox) & Cinzel Decorative/Playfair (Mirip Arabesque vibes) -->
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;700&family=Cinzel+Decorative:wght@400;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">

    <style>
        /* --- FONT SETUP (PENTING) --- */
        /* Jika kamu punya file font asli, uncomment bagian ini dan sesuaikan path-nya */
        /*
        @font-face {
            font-family: 'Vox Regular';
            src: url('./fonts/Vox-Regular.woff2') format('woff2');
        }
        @font-face {
            font-family: 'HWT Arabesque';
            src: url('./fonts/HWT-Arabesque.woff2') format('woff2');
        }
        */

        /* BASE STYLES */
        body { 
            font-family: 'Space Mono', monospace; 
            background-color: #0E0E0E; 
            color: #F0F0F0; 
            overflow-x: hidden;
            cursor: crosshair;
        }
        
        /* Font Classes Mapping (Fallback ke Google Fonts jika file asli belum ada) */
        .font-vox { font-family: 'Vox Regular', 'Space Grotesk', sans-serif; }
        .font-arabesque { font-family: 'HWT Arabesque', 'Cinzel Decorative', serif; }
        
        /* WARNA SPESIFIK */
        .color-0x { color: #E8E9ED; }
        .color-tanda { color: #F0F0F0; }
        .color-ungu { color: #836EF9; }
        .color-hijau { color: #00FF9D; }
        .bg-ungu { background-color: #836EF9; }
        .border-ungu { border-color: #836EF9; }
        
        /* ANIMATIONS */
        @keyframes marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }
        .animate-marquee { animation: marquee 40s linear infinite; }
        
        .fade-in-up { animation: fadeInUp 0.8s ease-out forwards; opacity: 0; transform: translateY(20px); }
        @keyframes fadeInUp { to { opacity: 1; transform: translateY(0); } }

        /* SCROLLBAR */
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #0E0E0E; }
        ::-webkit-scrollbar-thumb { background: #333; }
        ::-webkit-scrollbar-thumb:hover { background: #836EF9; }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        // --- ICONS ---
        const Icon = ({ path, size=24, className }) => (
            <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                {path}
            </svg>
        );
        
        const icons = {
            bag: <><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></>,
            zap: <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>,
            arrowRight: <><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></>,
            wallet: <><path d="M21 12V7H5a2 2 0 0 1 0-4h14v4"/><path d="M3 5v14a2 2 0 0 0 2 2h16v-5"/><path d="M18 12a2 2 0 0 0 0 4h4v-4Z"/></>,
            menu: <><line x1="4" x2="20" y1="12" y2="12"/><line x1="4" x2="20" y1="6" y2="6"/><line x1="4" x2="20" y1="18" y2="18"/></>,
            chevronRight: <path d="m9 18 6-6-6-6"/>,
            check: <><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></>,
            phone: <><rect width="14" height="20" x="5" y="2" rx="2" ry="2"/><path d="M12 18h.01"/></>,
            fingerprint: <><path d="M2 12C2 6.5 6.5 2 12 2a10 10 0 0 1 8 6"/><path d="M5 15.1a8.6 8.6 0 0 1 4.9-6.3 8.5 8.5 0 0 1 7.2 1.3"/><path d="M5.6 11.2a6 6 0 0 1 13.3-2.6"/><path d="M8 20.2a6 6 0 0 1 9.9-4.3"/><path d="M6.2 17.6a4.4 4.4 0 0 1 10.4-3.4"/><path d="M12 22a7 7 0 0 1-7-7"/></>,
            trash: <><path d="M3 6h18"/><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"/><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"/><line x1="10" x2="10" y1="11" y2="17"/><line x1="14" x2="14" y1="11" y2="17"/></>,
            loader: <path d="M21 12a9 9 0 1 1-6.219-8.56"/>,
            qr: <><rect width="5" height="5" x="3" y="3" rx="1"/><rect width="5" height="5" x="16" y="3" rx="1"/><rect width="5" height="5" x="3" y="16" rx="1"/><path d="M21 16h-3a2 2 0 0 0-2 2v3"/><path d="M21 21v.01"/><path d="M12 7v3a2 2 0 0 1-2 2H7"/><path d="M3 12h.01"/><path d="M12 3h.01"/><path d="M12 16v.01"/><path d="M16 12h1"/><path d="M21 12v.01"/><path d="M12 21v-1"/></>,
            layers: <><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></>,
            shield: <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
        };

        // --- DATA PRODUK ---
        const PRODUCTS = [
            {
                id: 1,
                name: "GENESIS TEE // BLACK",
                priceMON: 15,
                priceIDR: 249000,
                type: "T-Shirt",
                desc: "Cotton 24s Heavyweight. Sablon Plastisol '0x' di dada kiri. QR Code tersembunyi di label dalam.",
                nftBenefit: "Animasi Logo 3D + Whitelist Batch 002",
                imgPhysical: "https://placehold.co/600x800/1a1a1a/white?text=GENESIS+TEE"
            },
            {
                id: 2,
                name: "PROTOKOL HOODIE",
                priceMON: 35,
                priceIDR: 499000,
                type: "Hoodie",
                desc: "Oversized Charcoal. Patch NFC di lengan kiri. Tali ungu neon. Bahan Fleece tebal anti-angin.",
                nftBenefit: "Avatar Robot PFP + Akses Discord VIP",
                imgPhysical: "https://placehold.co/600x800/2a2a2a/white?text=PROTOKOL+HOODIE"
            },
            {
                id: 3,
                name: "ENKRIPSI CAP",
                priceMON: 10,
                priceIDR: 189000,
                type: "Accessories",
                desc: "Dad hat profile. Bordir logo '//' di depan. Strap adjustable.",
                nftBenefit: "Kartu Akses Digital (Diskon 5% Seumur Hidup)",
                imgPhysical: "https://placehold.co/600x800/000000/white?text=ENKRIPSI+CAP"
            }
        ];

        // --- LOGO COMPONENT (PLACEHOLDER IMAGE) ---
        // Ganti URL 'src' di bawah ini dengan file logo asli kamu nanti (format PNG transparant recommended)
        const Logo = ({ className }) => (
            <img 
                src="https://placehold.co/400x100/0E0E0E/836EF9?text=LOGO+BRAND&font=montserrat" 
                alt="0xTanda Logo" 
                className={`cursor-pointer hover:opacity-80 transition object-contain ${className}`}
            />
        );

        // --- COMPONENTS ---

        const CustomCursor = () => {
            const [position, setPosition] = React.useState({ x: 0, y: 0 });
            const [clicked, setClicked] = React.useState(false);

            React.useEffect(() => {
                const mMove = (el) => setPosition({ x: el.clientX, y: el.clientY });
                const mDown = () => setClicked(true);
                const mUp = () => setClicked(false);
                document.addEventListener("mousemove", mMove);
                document.addEventListener("mousedown", mDown);
                document.addEventListener("mouseup", mUp);
                return () => {
                    document.removeEventListener("mousemove", mMove);
                    document.removeEventListener("mousedown", mDown);
                    document.removeEventListener("mouseup", mUp);
                };
            }, []);

            return (
                <div className="fixed top-0 left-0 pointer-events-none z-[9999] hidden md:flex items-center justify-center transition-transform duration-75 ease-out" style={{ transform: `translate(${position.x}px, ${position.y}px)`, left: '-16px', top: '-16px' }}>
                    <div className={`w-8 h-8 border border-ungu rounded-full flex items-center justify-center transition-all duration-200 ${clicked ? 'scale-75 bg-ungu/20' : 'scale-100'}`}>
                        <div className="w-1 h-1 bg-ungu rounded-full"></div>
                    </div>
                    <div className="absolute w-12 h-[1px] bg-ungu/50"></div>
                    <div className="absolute h-12 w-[1px] bg-ungu/50"></div>
                </div>
            );
        };

        const Marquee = () => (
            <div className="bg-ungu text-black font-vox text-xs py-1 overflow-hidden whitespace-nowrap border-b border-ungu select-none">
                <div className="animate-marquee inline-block font-bold tracking-wider">
                    // 0xTanda// BATCH 001: GENESIS LIVE // ONE ENTITY // DUAL EXISTENCE // PHYGITAL STREETWEAR INDONESIA // 0xTanda// BATCH 001: GENESIS LIVE //
                </div>
            </div>
        );

        const Navbar = ({ setPage, cartCount, wallet, connectWallet }) => {
            const [menuOpen, setMenuOpen] = React.useState(false);
            return (
                <nav className="fixed top-8 left-0 w-full z-50 px-6 mix-blend-difference text-[#F0F0F0]">
                    <div className="flex justify-between items-center max-w-7xl mx-auto">
                        <div onClick={() => setPage('home')}>
                            {/* LOGO KECIL DI NAVBAR */}
                            <Logo className="h-8 md:h-10" />
                        </div>
                        <div className="hidden md:flex gap-8 font-mono text-sm">
                            <button onClick={() => setPage('shop')} className="hover:text-[#836EF9] hover:underline decoration-[#836EF9] underline-offset-4">SHOP</button>
                            <button onClick={() => setPage('verify')} className="hover:text-[#836EF9] hover:underline decoration-[#836EF9] underline-offset-4">CLAIM NFT</button>
                            <button onClick={() => setPage('about')} className="hover:text-[#836EF9] hover:underline decoration-[#836EF9] underline-offset-4">ABOUT US</button>
                        </div>
                        <div className="flex items-center gap-6">
                            <button onClick={connectWallet} className={`hidden md:flex items-center gap-2 border px-4 py-1 text-xs font-mono transition-all ${wallet ? 'border-ungu text-ungu bg-ungu/10' : 'border-white/20 hover:border-white'}`}>
                                <Icon path={icons.wallet} size={14} className="mr-2" />{wallet ? '0x...8821' : 'CONNECT'}
                            </button>
                            <div className="relative cursor-pointer hover:text-[#836EF9]" onClick={() => setPage('checkout')}>
                                <Icon path={icons.bag} size={20} />
                                {cartCount > 0 && <span className="absolute -top-2 -right-2 bg-ungu text-black text-[10px] w-4 h-4 flex items-center justify-center font-bold rounded-sm">{cartCount}</span>}
                            </div>
                            <div className="md:hidden cursor-pointer" onClick={() => setMenuOpen(!menuOpen)}>
                                <Icon path={icons.menu} size={24} />
                            </div>
                        </div>
                    </div>
                    {menuOpen && (
                        <div className="absolute top-12 left-0 w-full bg-[#0E0E0E] border-b border-[#333] p-6 md:hidden flex flex-col gap-4 font-mono text-sm shadow-xl z-50">
                            <button onClick={() => { setPage('shop'); setMenuOpen(false); }}>SHOP</button>
                            <button onClick={() => { setPage('verify'); setMenuOpen(false); }}>CLAIM NFT</button>
                            <button onClick={() => { setPage('about'); setMenuOpen(false); }}>ABOUT US</button>
                            <button onClick={() => { setPage('checkout'); setMenuOpen(false); }}>KERANJANG ({cartCount})</button>
                        </div>
                    )}
                </nav>
            );
        };

        // --- PAGES ---

        const LandingPage = ({ setPage }) => (
            <div className="relative w-full h-screen flex flex-col items-center justify-center overflow-hidden bg-[#0E0E0E]">
                <div className="absolute inset-0 opacity-20 pointer-events-none">
                    <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-ungu blur-[150px] rounded-full animate-pulse-slow"></div>
                    <div className="absolute w-full h-full bg-[url('https://www.transparenttextures.com/patterns/carbon-fibre.png')] opacity-30"></div>
                </div>
                <div className="z-10 text-center flex flex-col items-center gap-6 px-4 fade-in-up">
                    <div className="font-vox color-ungu text-xs tracking-[0.5em] uppercase font-bold">ONE ENTITY // DUAL EXISTENCE</div>
                    
                    {/* LOGO BESAR DI TENGAH */}
                    <div className="mb-4">
                        <Logo className="h-24 md:h-40" />
                    </div>
                    
                    <p className="font-mono text-[#999] max-w-md text-xs md:text-sm mb-10 leading-relaxed">SANDANG TERENKRIPSI.<br/>MILIKI ASETNYA DI MONAD CHAIN.</p>
                    <button onClick={() => setPage('shop')} className="group px-8 py-4 bg-transparent border border-[#F0F0F0] text-[#F0F0F0] font-mono text-sm hover:bg-ungu hover:border-ungu hover:text-black transition-all duration-300 flex items-center gap-2">COLLECTION <Icon path={icons.arrowRight} size={16} className="group-hover:translate-x-1 transition-transform"/></button>
                </div>
            </div>
        );

        const ShopPage = ({ addToCart, setPage, setSelectedProduct }) => (
            <div className="min-h-screen bg-[#0E0E0E] pt-28 pb-12 px-6">
                <div className="max-w-7xl mx-auto">
                    <div className="flex justify-between items-end mb-12 border-b border-[#333] pb-4">
                        <h2 className="text-3xl md:text-5xl font-bold text-[#F0F0F0] tracking-tight font-vox">SHOP 001 <span className="color-ungu text-sm font-mono ml-2 align-top">GENESIS</span></h2>
                        <div className="font-mono text-xs text-[#666] hidden md:block">STATUS: <span className="color-hijau">LIVE</span></div>
                    </div>
                    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                        {PRODUCTS.map((product) => (
                            <div key={product.id} className="group relative bg-[#121212] border border-[#333] hover:border-ungu transition-all duration-300 cursor-pointer" onClick={() => { setSelectedProduct(product); setPage('product'); }}>
                                <div className="relative aspect-[3/4] overflow-hidden bg-[#1a1a1a]">
                                    <img src={product.imgPhysical} alt={product.name} className="absolute inset-0 w-full h-full object-cover transition-opacity duration-300 group-hover:opacity-0" />
                                    <img src={product.imgNFT} alt="NFT" className="absolute inset-0 w-full h-full object-cover opacity-0 group-hover:opacity-100 scale-110 group-hover:scale-100 transition-all duration-500" />
                                    <div className="absolute top-4 right-4 bg-black/80 backdrop-blur-md border border-ungu text-ungu text-[10px] font-mono px-2 py-1 flex items-center gap-1"><Icon path={icons.zap} size={8} fill="currentColor"/> NFT</div>
                                </div>
                                <div className="p-4">
                                    <div className="flex justify-between items-start mb-2">
                                        <h3 className="font-bold text-[#F0F0F0] text-lg font-vox tracking-tighter">{product.name}</h3>
                                        <div className="text-right"><div className="color-ungu font-bold font-mono text-sm">{product.priceMON} MON</div><div className="text-[#666] text-[10px]">Rp {product.priceIDR.toLocaleString()}</div></div>
                                    </div>
                                    <div className="w-full h-[1px] bg-[#333] my-3 group-hover:bg-ungu transition-colors"></div>
                                    <div className="flex justify-between items-center text-xs font-mono text-[#666]"><span>{product.type}</span><span className="flex items-center gap-1 group-hover:text-[#F0F0F0]">DETAIL <Icon path={icons.chevronRight} size={12}/></span></div>
                                </div>
                            </div>
                        ))}
                    </div>
                </div>
            </div>
        );

        // ... [BAGIAN LITEPAPER/ABOUT SAMA SEPERTI SEBELUMNYA, HANYA FONT DISESUAIKAN] ...
        const AboutPage = () => {
            const Section = ({ title, children, className = "" }) => (
                <section className={`py-20 px-6 border-b border-[#333] ${className}`}>
                    <div className="max-w-4xl mx-auto">
                        {title && <h2 className="text-3xl md:text-5xl font-vox font-black mb-12 uppercase tracking-tighter text-ungu glitch-text" data-text={title}>{title}</h2>}
                        {children}
                    </div>
                </section>
            );

            return (
                <div className="bg-[#0E0E0E] text-[#F0F0F0] pt-20">
                    {/* HALAMAN 1: COVER */}
                    <section className="min-h-screen flex flex-col justify-center items-center text-center px-6 border-b border-[#333] relative overflow-hidden">
                        <div className="absolute inset-0 opacity-20 pointer-events-none"><img src="https://placehold.co/1920x1080/111/FFF?text=Texture+Kain" className="w-full h-full object-cover" /></div>
                        <div className="z-10 fade-in-up">
                            <div className="font-mono text-ungu text-xs tracking-[0.5em] mb-4">BRAND MANIFESTO</div>
                            
                            {/* LOGO BESAR DI ABOUT US */}
                            <div className="mb-6">
                                <Logo className="h-24 md:h-40 mx-auto" />
                            </div>
                            
                            <h2 className="text-xl md:text-3xl font-vox font-bold tracking-widest text-[#ccc] mb-8">ONE ENTITY // DUAL EXISTENCE</h2>
                            <p className="font-mono text-sm text-[#888]">Clothing First. Digital Second.<br/>Jakarta, Est. 2025</p>
                        </div>
                    </section>

                    {/* HALAMAN 2: THE PROBLEM */}
                    <Section>
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
                            <div><img src="https://placehold.co/600x800/1a1a1a/666?text=Generic+Fashion" className="w-full grayscale opacity-50 border border-[#333]" /></div>
                            <div>
                                <h3 className="text-4xl font-vox font-black mb-6 text-[#F0F0F0]">FASHION KEHILANGAN JIWA.</h3>
                                <div className="font-mono text-sm text-[#999] space-y-4 leading-relaxed border-l-2 border-[#333] pl-6">
                                    <p>Di era ini, kita membeli pakaian mahal, tetapi nilainya berhenti saat transaksi selesai. Tidak ada jejak sejarah. Tidak ada bukti keaslian yang abadi.</p>
                                    <p>Hanya kain yang perlahan usang.</p>
                                    <p className="text-[#F0F0F0] font-bold">Konsumen modern menginginkan lebih dari sekadar produk. Mereka menginginkan Identitas dan Kepemilikan Penuh.</p>
                                </div>
                            </div>
                        </div>
                    </Section>

                    {/* HALAMAN 3: THE PHILOSOPHY */}
                    <Section>
                        <div className="text-center mb-12">
                            <h3 className="text-4xl md:text-6xl font-vox font-black mb-6 uppercase">Satu Pakaian,<br/><span className="text-ungu">Dua Nyawa.</span></h3>
                            <p className="font-mono text-sm text-[#999] max-w-2xl mx-auto leading-relaxed">0xTanda bukan perusahaan teknologi. Kami adalah <strong>Brand Fashion</strong>. Fokus kami adalah menciptakan streetwear dengan kualitas terbaik—potongan (cut), bahan (fabric), dan detail yang sempurna.</p>
                        </div>
                        <div className="bg-[#121212] border border-[#333] p-8 max-w-3xl mx-auto text-center">
                            <p className="font-mono text-[#F0F0F0] text-sm">"Setiap fisik baju yang Anda beli, hadir dengan <span className="text-ungu">'Kembaran Digital'</span> (Sertifikat Hak Milik) secara GRATIS."</p>
                            <p className="text-xs text-[#666] mt-4">Ini bukan tentang spekulasi aset. Ini tentang validasi bahwa apa yang Anda kenakan adalah ASLI dan SEPENUHNYA MILIK ANDA.</p>
                        </div>
                    </Section>

                    {/* HALAMAN 4: THE PRODUCT */}
                    <Section title="CRAFTED FOR REALITY">
                        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
                            <div className="border border-[#333] p-6 bg-[#121212]"><div className="text-ungu mb-4"><Icon path={icons.layers} size={32}/></div><h4 className="font-bold text-[#F0F0F0] mb-2 font-vox">MATERIAL</h4><p className="text-xs text-[#888] font-mono">100% US Cotton Heavyweight (24s/20s) & Premium Fleece (330gsm).</p></div>
                            <div className="border border-[#333] p-6 bg-[#121212]"><div className="text-ungu mb-4"><Icon path={icons.shield} size={32}/></div><h4 className="font-bold text-[#F0F0F0] mb-2 font-vox">BUILD</h4><p className="text-xs text-[#888] font-mono">Jahitan rantai ganda, anti-shrink, potongan boxy fit modern.</p></div>
                            <div className="border border-[#333] p-6 bg-[#121212]"><div className="text-ungu mb-4"><Icon path={icons.zap} size={32}/></div><h4 className="font-bold text-[#F0F0F0] mb-2 font-vox">DETAIL</h4><p className="text-xs text-[#888] font-mono">Sablon High-Density Plastisol dan Bordir Komputer Presisi.</p></div>
                        </div>
                    </Section>

                    {/* HALAMAN 5: THE BONUS */}
                    <Section className="bg-[#121212]">
                        <div className="flex flex-col md:flex-row gap-12 items-center">
                            <div className="flex-1">
                                <h3 className="text-3xl font-vox font-black mb-6">YOUR DIGITAL RECEIPT.</h3>
                                <p className="font-mono text-sm text-[#999] mb-6">Lupakan bon kertas yang hilang dicuci. Setiap produk 0xTanda dilengkapi chip NFC tersembunyi.</p>
                                <ul className="space-y-4 font-mono text-sm text-[#F0F0F0]">
                                    <li className="flex gap-4 items-center"><span className="bg-ungu text-black w-6 h-6 flex items-center justify-center font-bold rounded-full">1</span> Scan: Tempelkan HP ke label baju.</li>
                                    <li className="flex gap-4 items-center"><span className="bg-ungu text-black w-6 h-6 flex items-center justify-center font-bold rounded-full">2</span> Claim: Dapatkan Sertifikat Digital (NFT).</li>
                                    <li className="flex gap-4 items-center"><span className="bg-ungu text-black w-6 h-6 flex items-center justify-center font-bold rounded-full">3</span> Own: Simpan, pamerkan, atau jual.</li>
                                </ul>
                                <div className="mt-8 text-xs text-[#666] border-t border-[#333] pt-4">Powered by Monad Chain (Invisible Tech). Teknologi blockchain hanya sebagai 'mesin pencatat'.</div>
                            </div>
                            <div className="flex-1 flex justify-center"><Icon path={icons.phone} size={150} className="text-[#333]"/></div>
                        </div>
                    </Section>

                    {/* HALAMAN 6: MEMBERSHIP */}
                    <Section className="bg-gradient-to-b from-[#0E0E0E] to-[#1a1a1a]">
                        <div className="max-w-3xl mx-auto text-center border border-ungu p-12 relative overflow-hidden">
                            <div className="absolute top-0 right-0 bg-ungu text-black text-xs font-bold px-4 py-1">LIMITED</div>
                            <h3 className="text-2xl md:text-4xl font-vox font-black mb-4">GENESIS 100</h3>
                            <p className="text-sm font-mono text-[#ccc] mb-8">THE FOUNDING MEMBERS</p>
                            <div className="text-left font-mono text-sm text-[#999] space-y-2 mb-8">
                                <p>Kami mencari 100 orang pertama yang percaya pada visi ini.</p>
                                <p>Pembeli koleksi Batch 1 (Genesis) akan mendapatkan status <strong>Founding Member</strong>.</p>
                            </div>
                            <div className="grid grid-cols-1 md:grid-cols-3 gap-4 text-xs font-bold text-[#F0F0F0]">
                                <div className="bg-[#333] p-3">🏆 Lifetime Discount</div>
                                <div className="bg-[#333] p-3">🔐 Priority Access</div>
                                <div className="bg-[#333] p-3">🤝 Community Voting</div>
                            </div>
                        </div>
                    </Section>

                    {/* HALAMAN 9: CONTACT */}
                    <section className="py-24 px-6 text-center bg-[#121212] border-t border-[#333]">
                        <h3 className="text-4xl font-vox font-black mb-8">KENAKAN KODE DI DUNIA NYATA.</h3>
                        <div className="flex justify-center gap-8 font-mono text-sm font-bold text-ungu">
                            <a href="#" className="hover:text-white">WEBSITE</a>
                            <a href="#" className="hover:text-white">INSTAGRAM</a>
                            <a href="#" className="hover:text-white">STORE (JKT)</a>
                        </div>
                        <div className="mt-12 opacity-50"><Icon path={icons.qr} size={80} className="mx-auto text-white"/></div>
                    </section>
                </div>
            );
        };

        const ProductDetail = ({ product, addToCart, setPage }) => {
            const [activeImg, setActiveImg] = React.useState('physical');
            if (!product) return null;
            return (
                <div className="min-h-screen bg-[#0E0E0E] pt-28 pb-12 px-6">
                    <div className="max-w-7xl mx-auto">
                        <button onClick={() => setPage('shop')} className="text-[#666] font-mono text-xs mb-8 hover:text-white flex items-center gap-2">← KEMBALI KE ARSIP</button>
                        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12">
                            <div className="relative aspect-square border border-[#333] bg-[#121212]">
                                <img src={activeImg === 'physical' ? product.imgPhysical : product.imgNFT} className="w-full h-full object-cover" alt="Detail" />
                                <div className="absolute bottom-6 left-1/2 -translate-x-1/2 flex bg-black border border-[#333] p-1 rounded-full shadow-lg">
                                    <button onClick={() => setActiveImg('physical')} className={`px-4 py-2 text-xs font-mono rounded-full transition-colors ${activeImg === 'physical' ? 'bg-[#F0F0F0] text-black' : 'text-[#666]'}`}>FISIK</button>
                                    <button onClick={() => setActiveImg('nft')} className={`px-4 py-2 text-xs font-mono rounded-full transition-colors flex items-center gap-1 ${activeImg === 'nft' ? 'bg-ungu text-black' : 'text-[#666]'}`}>DIGITAL <Icon path={icons.zap} size={10}/></button>
                                </div>
                            </div>
                            <div className="flex flex-col justify-center text-[#F0F0F0]">
                                <h1 className="text-4xl md:text-6xl font-vox font-black mb-2 tracking-tighter uppercase">{product.name}</h1>
                                <div className="flex gap-4 font-mono text-sm text-ungu mb-8"><span className="border border-ungu px-2 py-1">BATCH 001</span><span className="border border-ungu px-2 py-1">STOK: 100 PCS</span></div>
                                <p className="text-[#999] font-mono mb-8 leading-relaxed text-sm">{product.desc}<br/><br/><span className="text-[#F0F0F0] font-bold border-b border-[#F0F0F0] pb-1">BENEFIT NFT:</span><br/>{product.nftBenefit}</p>
                                <div className="space-y-4">
                                    <button onClick={() => addToCart(product)} className="w-full py-4 bg-ungu text-black font-bold font-mono text-lg hover:bg-[#6f5bd6] transition-colors flex items-center justify-center gap-2"><Icon path={icons.wallet} size={18} /> BELI PAKAI MON ({product.priceMON})</button>
                                    <button onClick={() => addToCart(product)} className="w-full py-4 bg-transparent border border-[#333] text-[#F0F0F0] font-mono hover:border-white transition-colors">BELI PAKAI RUPIAH (Rp {product.priceIDR.toLocaleString()})</button>
                                </div>
                                <div className="mt-6 text-xs text-[#666] font-mono flex gap-4"><span className="flex items-center gap-1"><Icon path={icons.check} size={10} className="color-hijau"/> Garansi NFC</span><span className="flex items-center gap-1"><Icon path={icons.check} size={10} className="color-hijau"/> Verifikasi Blockchain</span></div>
                            </div>
                        </div>
                    </div>
                </div>
            );
        };

        const CheckoutPage = ({ cart, setCart, setPage, wallet, connectWallet }) => {
            const [step, setStep] = React.useState(1);
            const [formData, setFormData] = React.useState({ name: '', address: '', city: '', phone: '', email: '' });
            const [paymentMethod, setPaymentMethod] = React.useState('mon');
            const [isProcessing, setIsProcessing] = React.useState(false);
            const totalMON = cart.reduce((acc, item) => acc + item.priceMON, 0);
            const totalIDR = cart.reduce((acc, item) => acc + item.priceIDR, 0);

            const removeItem = (index) => {
                const newCart = [...cart];
                newCart.splice(index, 1);
                setCart(newCart);
            };

            const handleProcessPayment = () => {
                if (paymentMethod === 'mon' && !wallet) {
                    alert("Harap sambungkan Wallet Monad Anda terlebih dahulu!");
                    connectWallet();
                    return;
                }
                setIsProcessing(true);
                setTimeout(() => {
                    setIsProcessing(false);
                    setStep(4);
                    setCart([]); 
                }, 3500);
            };

            if (cart.length === 0 && step === 1) {
                return (
                    <div className="min-h-screen bg-[#0E0E0E] flex flex-col items-center justify-center p-6 text-center pt-24">
                        <div className="w-20 h-20 border border-[#333] flex items-center justify-center rounded-full mb-6"><Icon path={icons.bag} size={32} className="text-[#666]" /></div>
                        <h2 className="text-[#F0F0F0] font-bold text-2xl font-mono mb-2">KERANJANG KOSONG</h2>
                        <button onClick={() => setPage('shop')} className="px-8 py-3 bg-[#F0F0F0] text-black font-mono font-bold hover:bg-ungu transition-colors">KEMBALI KE KOLEKSI</button>
                    </div>
                );
            }

            return (
                <div className="min-h-screen bg-[#0E0E0E] pt-28 pb-12 px-6 font-mono text-[#F0F0F0]">
                    <div className="max-w-4xl mx-auto">
                        <div className="flex justify-between mb-12 text-[10px] md:text-sm text-[#666] border-b border-[#333] pb-4 tracking-widest">
                            <span className={step >= 1 ? "text-ungu" : ""}>01 KERANJANG</span>
                            <span className={step >= 2 ? "text-ungu" : ""}>02 PENGIRIMAN</span>
                            <span className={step >= 3 ? "text-ungu" : ""}>03 PEMBAYARAN</span>
                            <span className={step >= 4 ? "color-hijau" : ""}>04 SELESAI</span>
                        </div>

                        {step === 1 && (
                            <div className="animate-fade-in">
                                <h1 className="text-3xl font-bold mb-8 font-vox uppercase">Review Pesanan</h1>
                                <div className="space-y-6 mb-8">
                                    {cart.map((item, idx) => (
                                        <div key={idx} className="flex gap-4 border border-[#333] p-4 bg-[#121212] items-center">
                                            <img src={item.imgPhysical} alt={item.name} className="w-20 h-20 object-cover border border-[#333]" />
                                            <div className="flex-1">
                                                <h3 className="font-bold text-sm md:text-lg text-[#F0F0F0]">{item.name}</h3>
                                                <div className="text-ungu text-xs flex items-center gap-1"><Icon path={icons.zap} size={10}/> Termasuk NFT</div>
                                            </div>
                                            <div className="text-right">
                                                <div className="font-bold text-[#F0F0F0]">{item.priceMON} MON</div>
                                                <div className="text-[#666] text-xs">Rp {item.priceIDR.toLocaleString()}</div>
                                                <button onClick={() => removeItem(idx)} className="text-[#666] mt-2 hover:text-red-500"><Icon path={icons.trash} size={16}/></button>
                                            </div>
                                        </div>
                                    ))}
                                </div>
                                <div className="flex justify-between items-center border-t border-[#333] pt-6 mb-8">
                                    <span className="text-[#666]">ESTIMASI TOTAL</span>
                                    <div className="text-right"><div className="text-2xl font-bold text-ungu">{totalMON} MON</div><div className="text-[#666]">atau Rp {totalIDR.toLocaleString()}</div></div>
                                </div>
                                <button onClick={() => setStep(2)} className="w-full bg-[#F0F0F0] text-black py-4 font-bold hover:bg-ungu transition-colors tracking-widest">LANJUT PENGIRIMAN</button>
                            </div>
                        )}

                        {step === 2 && (
                            <div className="animate-fade-in">
                                <h1 className="text-3xl font-bold mb-8 font-vox uppercase">Data Penerima</h1>
                                <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                                    <div className="space-y-2"><label className="text-xs text-[#666]">NAMA LENGKAP</label><input type="text" className="w-full bg-[#121212] border border-[#333] p-3 text-[#F0F0F0] focus:border-ungu outline-none" value={formData.name} onChange={(e) => setFormData({...formData, name: e.target.value})} placeholder="Nama Kamu" /></div>
                                    <div className="space-y-2"><label className="text-xs text-[#666]">EMAIL</label><input type="email" className="w-full bg-[#121212] border border-[#333] p-3 text-[#F0F0F0] focus:border-ungu outline-none" value={formData.email} onChange={(e) => setFormData({...formData, email: e.target.value})} /></div>
                                    <div className="space-y-2 md:col-span-2"><label className="text-xs text-[#666]">ALAMAT</label><input type="text" className="w-full bg-[#121212] border border-[#333] p-3 text-[#F0F0F0] focus:border-ungu outline-none" value={formData.address} onChange={(e) => setFormData({...formData, address: e.target.value})} /></div>
                                </div>
                                <div className="flex gap-4">
                                    <button onClick={() => setStep(1)} className="w-1/3 border border-[#333] text-[#666] hover:text-white py-4">KEMBALI</button>
                                    <button onClick={() => { if(!formData.name || !formData.address) return alert("Mohon lengkapi data."); setStep(3); }} className="w-2/3 bg-[#F0F0F0] text-black font-bold hover:bg-ungu">LANJUT PEMBAYARAN</button>
                                </div>
                            </div>
                        )}

                        {step === 3 && (
                            <div className="animate-fade-in">
                                <h1 className="text-3xl font-bold mb-8 font-vox uppercase">Pilih Pembayaran</h1>
                                <div className="grid grid-cols-2 gap-4 mb-8">
                                    <button onClick={() => setPaymentMethod('mon')} className={`p-6 border flex flex-col items-center justify-center gap-2 ${paymentMethod === 'mon' ? 'border-ungu bg-ungu/10 text-ungu' : 'border-[#333] text-[#666]'}`}><Icon path={icons.zap} size={24} /><span className="font-bold">BAYAR MON</span></button>
                                    <button onClick={() => setPaymentMethod('idr')} className={`p-6 border flex flex-col items-center justify-center gap-2 ${paymentMethod === 'idr' ? 'border-hijau bg-hijau/10 color-hijau' : 'border-[#333] text-[#666]'}`}><Icon path={icons.qr} size={24} /><span className="font-bold">BAYAR IDR</span></button>
                                </div>
                                <div className="flex gap-4">
                                    <button onClick={() => setStep(2)} className="w-1/3 border border-[#333] text-[#666] hover:text-white py-4">KEMBALI</button>
                                    <button onClick={handleProcessPayment} disabled={isProcessing} className="w-2/3 bg-ungu text-black font-bold flex items-center justify-center gap-2">{isProcessing ? <><Icon path={icons.loader} size={18} className="animate-spin"/> PROSES...</> : 'BAYAR SEKARANG'}</button>
                                </div>
                            </div>
                        )}

                        {step === 4 && (
                            <div className="animate-scale-in text-center flex flex-col items-center justify-center py-12">
                                <div className="w-24 h-24 bg-[#00FF9D]/10 border border-[#00FF9D] rounded-full flex items-center justify-center mb-6"><Icon path={icons.check} size={48} className="text-[#00FF9D]" /></div>
                                <h1 className="text-4xl md:text-5xl font-bold mb-4 font-vox text-[#F0F0F0]">PESANAN DITERIMA</h1>
                                <button onClick={() => { setStep(1); setPage('home'); }} className="px-8 py-3 border border-[#F0F0F0] text-[#F0F0F0] hover:bg-[#F0F0F0] hover:text-black">KEMBALI KE BERANDA</button>
                            </div>
                        )}
                    </div>
                </div>
            );
        };

        const VerifyPage = ({ wallet, connectWallet }) => {
            const [status, setStatus] = React.useState('idle');
            return (
                <div className="min-h-screen bg-[#0E0E0E] flex flex-col pt-24 items-center justify-center p-6 text-center text-[#F0F0F0]">
                    {status === 'idle' && (
                        <div className="animate-fade-in">
                            <div className="w-24 h-24 border-2 border-[#333] rounded-full flex items-center justify-center mx-auto mb-8 relative"><Icon path={icons.phone} size={40} className="text-[#666]" /><div className="absolute inset-0 rounded-full animate-ping bg-ungu/10"></div></div>
                            <h2 className="text-2xl font-bold mb-4 font-mono">SCAN CHIP NFC</h2>
                            <button onClick={() => { setStatus('scanning'); setTimeout(() => setStatus('verified'), 3000); }} className="bg-[#F0F0F0] text-black px-8 py-3 font-mono font-bold hover:bg-white rounded-sm">SIMULASI TEMPEL</button>
                        </div>
                    )}
                    {status === 'scanning' && <div className="flex flex-col items-center"><div className="w-32 h-32 border border-ungu rounded-full flex items-center justify-center animate-pulse mb-6 bg-ungu/5"><Icon path={icons.fingerprint} size={48} className="text-ungu animate-pulse" /></div><div className="font-mono text-ungu">MEMBACA DATA CHIP...</div></div>}
                    {status === 'verified' && (
                        <div className="animate-scale-in max-w-md w-full">
                            <div className="w-16 h-16 bg-[#00FF9D]/10 rounded-full flex items-center justify-center mx-auto mb-4 border border-[#00FF9D]"><Icon path={icons.check} size={32} className="text-[#00FF9D]" /></div>
                            <h2 className="text-3xl font-black mb-2 uppercase text-[#F0F0F0]">PRODUK ASLI</h2>
                            {!wallet ? <button onClick={connectWallet} className="w-full bg-ungu text-black py-4 font-bold font-mono animate-pulse">SAMBUNGKAN WALLET UNTUK KLAIM</button> : <div className="text-[#F0F0F0] font-mono border border-[#00FF9D] p-4 bg-[#00FF9D]/10">NFT SIAP DI-MINT KE 0x...8821</div>}
                        </div>
                    )}
                </div>
            );
        };

        const App = () => {
            const [page, setPage] = React.useState('home');
            const [selectedProduct, setSelectedProduct] = React.useState(null);
            const [cart, setCart] = React.useState([]);
            const [walletConnected, setWalletConnected] = React.useState(false);

            const addToCart = (product) => {
                setCart([...cart, product]);
                setPage('checkout'); 
            };

            const connectWallet = () => {
                if(!walletConnected) {
                    if(window.confirm("Simulasi: Sambungkan Wallet Monad?")) setWalletConnected(true);
                } else {
                    setWalletConnected(false);
                }
            };

            return (
                <div className="bg-[#0E0E0E] min-h-screen text-[#F0F0F0] selection:bg-ungu selection:text-black font-mono cursor-none">
                    <CustomCursor />
                    <Marquee />
                    <Navbar setPage={setPage} cartCount={cart.length} wallet={walletConnected} connectWallet={connectWallet} />
                    <main className="relative z-10 fade-in-up">
                        {page === 'home' && <LandingPage setPage={setPage} />}
                        {page === 'shop' && <ShopPage addToCart={addToCart} setPage={setPage} setSelectedProduct={setSelectedProduct} />}
                        {page === 'product' && <ProductDetail product={selectedProduct} addToCart={addToCart} setPage={setPage} />}
                        {page === 'checkout' && <CheckoutPage cart={cart} setCart={setCart} setPage={setPage} wallet={walletConnected} connectWallet={connectWallet} />}
                        {page === 'verify' && <VerifyPage wallet={walletConnected} connectWallet={connectWallet} />}
                        {page === 'about' && <AboutPage />}
                    </main>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>